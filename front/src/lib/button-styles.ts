/**
 * 공용 버튼 스타일 상수.
 * 컴포넌트가 아닌 클래스 문자열이라 서버/클라이언트 어느 쪽에서도 그대로 쓸 수 있다.
 * 여백(mt-* 등)은 쓰는 쪽 배치 책임이므로 포함하지 않는다.
 */
export const PRIMARY_CTA =
  "w-full rounded-lg bg-primary py-3 text-[15px] font-semibold text-primary-foreground transition-colors hover:bg-primary-hover disabled:cursor-not-allowed disabled:opacity-60";
